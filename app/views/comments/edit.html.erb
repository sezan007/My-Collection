<%=form_with(model: [@item.collection,@item,@comment],local: true ,remote: true) do |form|%>
    <div>

      <%= form.label :content %>
      <%= form.text_area :content %>

    </div>
    <%=form.submit 'Update Comment'%>
  <%end%>